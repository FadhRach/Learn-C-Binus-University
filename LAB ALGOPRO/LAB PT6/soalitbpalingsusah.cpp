#include <stdio.h>
#include <math.h>

int main() {
	
	int waktu1, waktu2, waktu3, tambahWaktu, poin;
	
	printf("Masukkan waktu tahap 1 (menit) : \a");
	scanf("%d", &waktu1); getchar();
	printf(" %d Masukkan waktu tahap 2 (menit) : ", 5^2);
	scanf("%d", &waktu2); getchar();
	printf("Masukkan waktu tahap 3 (menit) : ");
	scanf("%d", &waktu3); getchar();
	
	if(waktu1 <= 5){
		
	}
	
	
	
	
	return 0;
}